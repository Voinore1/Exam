<Window x:Class="Shop.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shop"
        mc:Ignorable="d"
        Title="Window2" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=local:ViewModel}">
    <ListBox ItemsSource="{Binding SelBookReviews}">
        <DataTemplate>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="{Binding User.Username}"></Label>
                <Label Grid.Row="0" Grid.Column="1" Content="{Binding Book.Title}"></Label>
                <Label Grid.Row="0" Grid.Column="2" Content="{Binding Rating}"></Label>
                <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Content="{Binding Reviews}"></Label>
            </Grid>
        </DataTemplate>
    </ListBox>
</Window>
